# Load Test Results

This directory stores load test outputs generated by `scripts/load-test.sh`.

Each run writes a timestamped text report here so the results stay with the project instead of being lost under `/tmp`.

Current reports:

- `load-test-results-20260310-heavy-vegeta-1000rps-300s.txt` (heavy run)
- `load-test-results-20260310-183653.txt` (baseline run)
